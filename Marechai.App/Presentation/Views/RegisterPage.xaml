<?xml version="1.0"
      encoding="utf-8"?>

<Page x:Class="Marechai.App.Presentation.Views.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:Marechai.App.Presentation.ViewModels"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      d:DataContext="{d:DesignInstance vm:RegisterViewModel}">

    <Grid utu:SafeArea.Insets="VisibleBounds">
        <Grid HorizontalAlignment="Center"
              VerticalAlignment="Center"
              MaxWidth="520"
              Padding="32">

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="8"
                    Padding="0"
                    Translation="0,0,16">
                <Border.Shadow>
                    <ThemeShadow />
                </Border.Shadow>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0"
                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                            Padding="16,12"
                            CornerRadius="6,6,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <FontIcon Grid.Column="0"
                                      Glyph="&#xE8FA;"
                                      FontSize="20"
                                      Foreground="White"
                                      VerticalAlignment="Center"
                                      Margin="0,0,12,0" />

                            <TextBlock Grid.Column="1"
                                       Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_Title}"
                                       Foreground="White"
                                       FontWeight="SemiBold"
                                       FontSize="18"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </Border>

                    <!-- Body -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="24"
                                  MaxHeight="640">
                        <StackPanel Spacing="16">

                            <!-- Pre-submit form -->
                            <StackPanel Spacing="14"
                                        Visibility="{Binding Submitted, Converter={StaticResource InvertBoolToVisibilityConverter}}">
                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_Intro}"
                                           TextWrapping="Wrap"
                                           Style="{StaticResource BodyTextBlockStyle}" />

                                <InfoBar IsOpen="{Binding HasError, Mode=OneWay}"
                                         Severity="Error"
                                         Message="{Binding ErrorMessage}"
                                         IsClosable="False" />

                                <StackPanel Spacing="6">
                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_InvitationCodeLabel}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBox Text="{Binding InvitationCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             PlaceholderText="XXXX-YYYY" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_EmailLabel}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBox Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             InputScope="EmailNameOrAddress" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_UserNameLabel}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBox Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_DisplayNameLabel}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBox Text="{Binding DisplayName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_DisplayNameHelp}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_PasswordLabel}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <PasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=RegisterPage_ConfirmPasswordLabel}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <PasswordBox Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <Button Content="{Binding Source={StaticResource Strings}, Path=RegisterPage_SubmitButton}"
                                        Command="{Binding SubmitCommand}"
                                        IsEnabled="{Binding IsSubmitting, Converter={StaticResource InvertBoolConverter}}"
                                        HorizontalAlignment="Stretch"
                                        Style="{StaticResource AccentButtonStyle}" />
                            </StackPanel>

                            <!-- Post-submit confirmation -->
                            <StackPanel Spacing="16"
                                        Visibility="{Binding Submitted, Converter={StaticResource BoolToVisibilityConverter}}">
                                <InfoBar IsOpen="True"
                                         Severity="Success"
                                         Title="{Binding Source={StaticResource Strings}, Path=RegisterPage_ConfirmationTitle}"
                                         Message="{Binding Source={StaticResource Strings}, Path=RegisterPage_ConfirmationMessage}"
                                         IsClosable="False" />
                                <HyperlinkButton Content="{Binding Source={StaticResource Strings}, Path=RegisterPage_ResendLink}"
                                                 Command="{Binding ResendConfirmationCommand}"
                                                 HorizontalAlignment="Center" />
                            </StackPanel>

                            <HyperlinkButton Content="{Binding Source={StaticResource Strings}, Path=RegisterPage_BackToLogin}"
                                             Command="{Binding BackToLoginCommand}"
                                             HorizontalAlignment="Center" />
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>

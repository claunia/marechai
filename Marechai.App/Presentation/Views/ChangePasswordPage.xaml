<?xml version="1.0"
      encoding="utf-8"?>

<Page x:Class="Marechai.App.Presentation.Views.ChangePasswordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:Marechai.App.Presentation.ViewModels"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      d:DataContext="{d:DesignInstance vm:ChangePasswordViewModel}">

    <Grid utu:SafeArea.Insets="VisibleBounds">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="520" Padding="32">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{ThemeResource AccentFillColorDefaultBrush}" Padding="16,12" CornerRadius="6,6,0,0">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE72E;" FontSize="20" Foreground="White" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Source={StaticResource Strings}, Path=ChangePasswordPage_Title}"
                                       Foreground="White" FontWeight="SemiBold" FontSize="18" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>

                    <ScrollViewer Grid.Row="1" Padding="24" VerticalScrollBarVisibility="Auto">
                        <StackPanel Spacing="16">
                            <InfoBar IsOpen="{Binding HasError, Mode=OneWay}" Severity="Error"
                                     Message="{Binding ErrorMessage}" IsClosable="False" />

                            <InfoBar IsOpen="{Binding HasSuccess, Mode=OneWay}" Severity="Success"
                                     Message="{Binding SuccessMessage}" IsClosable="False" />

                            <StackPanel Spacing="6">
                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=ChangePasswordPage_CurrentPasswordLabel}"
                                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <PasswordBox Password="{Binding CurrentPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <StackPanel Spacing="6">
                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=ChangePasswordPage_NewPasswordLabel}"
                                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <PasswordBox Password="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=ChangePasswordPage_HelperText}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>

                            <StackPanel Spacing="6">
                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=ChangePasswordPage_ConfirmPasswordLabel}"
                                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <PasswordBox Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <Button Content="{Binding Source={StaticResource Strings}, Path=ChangePasswordPage_SubmitButton}"
                                    Command="{Binding SubmitCommand}"
                                    IsEnabled="{Binding IsSubmitting, Converter={StaticResource InvertBoolConverter}}"
                                    HorizontalAlignment="Stretch"
                                    Style="{ThemeResource AccentButtonStyle}" />

                            <HyperlinkButton Content="{Binding Source={StaticResource Strings}, Path=ChangePasswordPage_BackLink}"
                                             Command="{Binding BackToSettingsCommand}"
                                             HorizontalAlignment="Center" />
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>

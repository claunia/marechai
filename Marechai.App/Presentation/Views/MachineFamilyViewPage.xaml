<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Marechai.App.Presentation.Views.MachineFamilyViewPage"
      x:Name="PageRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid utu:SafeArea.Insets="VisibleBounds">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <utu:NavigationBar Grid.Row="0"
                           Content="{Binding Path=Title}"
                           MainCommandMode="Action">
            <utu:NavigationBar.MainCommand>
                <AppBarButton Label="{Binding Source={StaticResource Strings}, Path=BackButton}" Icon="Back"
                              Command="{Binding GoBackCommand}"
                              AutomationProperties.Name="Go back" />
            </utu:NavigationBar.MainCommand>
        </utu:NavigationBar>

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="16"
                        Spacing="16">

                <!-- Loading State -->
                <StackPanel Visibility="{Binding IsLoading}"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Padding="32"
                            Spacing="16">
                    <ProgressRing IsActive="True"
                                  IsIndeterminate="True"
                                  Height="48"
                                  Width="48" />
                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=LoadingText}"
                               TextAlignment="Center"
                               FontSize="14" />
                </StackPanel>

                <!-- Error State -->
                <StackPanel Visibility="{Binding HasError}"
                            Padding="16"
                            Background="{ThemeResource SystemErrorBackgroundColor}"
                            CornerRadius="8"
                            Spacing="8">
                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=ErrorText}"
                               FontWeight="Bold"
                               Foreground="{ThemeResource SystemErrorTextForegroundColor}" />
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{ThemeResource SystemErrorTextForegroundColor}"
                               TextWrapping="Wrap" />
                </StackPanel>

                <!-- Family Details -->
                <StackPanel Visibility="{Binding IsDataLoaded}"
                            Spacing="16">

                    <!-- Header card with shared theme-aware hero gradient, mirrors the Blazor family page -->
                    <Border Style="{StaticResource HeroHeaderBorderStyle}">
                        <StackPanel Spacing="4">
                            <TextBlock Text="{Binding FamilyName}"
                                       Style="{StaticResource HeroTitleTextStyle}"
                                       FontSize="22" />

                            <!-- Company as a link -->
                            <HyperlinkButton Visibility="{Binding ShowCompanyLink, Converter={StaticResource BoolToVisibilityConverter}}"
                                             Command="{Binding NavigateToCompanyCommand}"
                                             Padding="0"
                                             HorizontalAlignment="Left">
                                <TextBlock Text="{Binding CompanyName}"
                                           FontSize="16"
                                           Foreground="{ThemeResource HeroForegroundBrush}"
                                           TextWrapping="Wrap" />
                            </HyperlinkButton>

                            <!-- Company as plain text (no company link available) -->
                            <TextBlock Visibility="{Binding ShowCompanyLink, Converter={StaticResource InvertBoolToVisibilityConverter}}"
                                       Text="{Binding CompanyName}"
                                       Style="{StaticResource HeroSubtitleTextStyle}"
                                       FontSize="16" />
                        </StackPanel>
                    </Border>

                    <!-- Machine count -->
                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding ShowMachines, Converter={StaticResource BoolToVisibilityConverter}}"
                                Spacing="8">
                        <TextBlock Text="{Binding Machines.Count}"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="{ThemeResource SystemAccentColor}" />
                        <TextBlock Text="{Binding Source={StaticResource Strings}, Path=MachinesFoundInFamilyText}"
                                   FontSize="14"
                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                    </StackPanel>

                    <!-- Machines List -->
                    <StackPanel Visibility="{Binding ShowMachines, Converter={StaticResource BoolToVisibilityConverter}}"
                                Spacing="8">
                        <ItemsRepeater ItemsSource="{Binding Machines}"
                                       Margin="0">
                            <ItemsRepeater.Layout>
                                <StackLayout Spacing="4" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.NavigateToMachineCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            Padding="12,8"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="14"
                                                   TextWrapping="Wrap" />
                                    </Button>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                    <!-- Empty State -->
                    <Border Visibility="{Binding ShowEmptyMachines, Converter={StaticResource BoolToVisibilityConverter}}"
                            Style="{StaticResource MetadataCardStyle}">
                        <TextBlock Text="{Binding Source={StaticResource Strings}, Path=NoMachinesInFamilyText}"
                                   TextWrapping="Wrap" />
                    </Border>

                </StackPanel>

            </StackPanel>
        </ScrollViewer>

    </Grid>

</Page>

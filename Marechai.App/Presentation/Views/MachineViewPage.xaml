<?xml version="1.0"
      encoding="utf-8"?>

<Page x:Class="Marechai.App.Presentation.Views.MachineViewPage"
      x:Name="PageRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:loc="using:Marechai.App.Presentation.Converters"
      xmlns:wctui="using:CommunityToolkit.WinUI.UI.Controls"
      xmlns:local="using:Marechai.App.Presentation.Views"
      xmlns:components="using:Marechai.App.Presentation.Components"
      NavigationCacheMode="Disabled"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header with Back Button -->
        <Grid Grid.Row="0"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1"
              Padding="12,12,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" /> <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Back Button -->
            <Button Grid.Column="0"
                    Command="{Binding GoBackCommand}"
                    Style="{ThemeResource AlternateButtonStyle}"
                    ToolTipService.ToolTip="Go back"
                    Padding="8"
                    MinWidth="44"
                    MinHeight="44"
                    VerticalAlignment="Center">
                <FontIcon Glyph="&#xE72B;"
                          FontSize="16" />
            </Button>

            <!-- Title Section -->
            <StackPanel Grid.Column="1"
                        Margin="12,0,0,0"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding MachineName}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{ThemeResource TextControlForeground}"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">

            <!-- Loading State -->
            <StackPanel Visibility="{Binding IsLoading}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Padding="32"
                        Spacing="16">
                <ProgressRing IsActive="True"
                              IsIndeterminate="True"
                              Height="64"
                              Width="64"
                              Foreground="{ThemeResource SystemAccentColor}" />
                <TextBlock
                           Text="{Binding Source={StaticResource Strings}, Path=LoadingText}"
                           FontSize="14"
                           TextAlignment="Center"
                           Foreground="{ThemeResource SystemBaseMediumColor}" />
            </StackPanel>

            <!-- Error State -->
            <StackPanel Visibility="{Binding HasError}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Padding="24"
                        Spacing="16"
                        MaxWidth="400">
                <InfoBar IsOpen="True"
                         Severity="Error"
                         Title="{Binding Source={StaticResource Strings}, Path=MachineErrorInfoBar_Title}"
                         Message="{Binding ErrorMessage}"
                         IsClosable="False" />
                <Button
                        Content="{Binding Source={StaticResource Strings}, Path=RetryButton}"
                        Command="{Binding LoadData}"
                        HorizontalAlignment="Center"
                        Style="{ThemeResource AccentButtonStyle}" />
            </StackPanel>

            <!-- Machine Details -->
            <Grid Visibility="{Binding IsDataLoaded}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <components:HeroHeader Grid.Row="0"
                                       Margin="16,16,16,0"
                                       HeroTitle="{Binding MachineName}"
                                       HeroSubtitle="{Binding CompanyName}" />

                <TabView Grid.Row="1"
                         Style="{StaticResource DetailTabViewStyle}">

                    <TabViewItem Style="{StaticResource DetailTabViewItemStyle}"
                                 Header="{Binding Source={StaticResource Strings}, Path=SpecificationsTabHeader}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Spacing="24"
                                            Padding="0,16,0,0">

                    <!-- Prototype Badge -->
                    <StackPanel Visibility="{Binding IsPrototype}">
                        <Border Style="{StaticResource ChipWarningStyle}"
                                Padding="16,12"
                                HorizontalAlignment="Left">
                            <TextBlock
                                       Text="{Binding Source={StaticResource Strings}, Path=PrototypeBadgeText}"
                                       Style="{StaticResource ChipTextStyle}"
                                       Foreground="{ThemeResource WarningForegroundColorDefaultBrush}" />
                        </Border>
                    </StackPanel>

                    <!-- Introduction Date -->
                    <StackPanel Visibility="{Binding ShowIntroductionDate}"
                                Spacing="8">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=IntroductionDateLabel}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                        <Border Style="{StaticResource MetadataCardStyle}">
                            <TextBlock Text="{Binding IntroductionDateDisplay}"
                                       FontSize="16"
                                       Foreground="{ThemeResource TextControlForeground}" />
                        </Border>
                    </StackPanel>

                    <!-- Family and Model -->
                    <Grid Visibility="{Binding ShowFamilyOrModel}"
                          ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /> <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Family -->
                        <StackPanel Grid.Column="0"
                                    Visibility="{Binding ShowFamily}"
                                    Spacing="8">
                            <TextBlock
                                       Text="{Binding Source={StaticResource Strings}, Path=FamilyLabel}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource SystemBaseMediumColor}" />
                            <Border Style="{StaticResource MetadataCardStyle}">
                                <HyperlinkButton Command="{Binding NavigateToFamilyCommand}"
                                                 Padding="0"
                                                 HorizontalAlignment="Left"
                                                 HorizontalContentAlignment="Left">
                                    <TextBlock Text="{Binding FamilyName}"
                                               FontSize="14"
                                               TextTrimming="CharacterEllipsis" />
                                </HyperlinkButton>
                            </Border>
                        </StackPanel>

                        <!-- Model -->
                        <StackPanel Grid.Column="1"
                                    Visibility="{Binding ShowModel}"
                                    Spacing="8">
                            <TextBlock
                                       Text="{Binding Source={StaticResource Strings}, Path=ModelLabel}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource SystemBaseMediumColor}" />
                            <Border Style="{StaticResource MetadataCardStyle}">
                                <TextBlock Text="{Binding ModelName}"
                                           FontSize="14"
                                           Foreground="{ThemeResource TextControlForeground}"
                                           TextTrimming="CharacterEllipsis" />
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- Processors Section -->
                    <StackPanel Visibility="{Binding ShowProcessors}"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=ProcessorsLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <ItemsControl ItemsSource="{Binding Processors}"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="8" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Padding="0"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            Command="{Binding DataContext.NavigateToProcessorCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0">
                                        <Border Style="{StaticResource MetadataCardStyle}">
                                            <StackPanel Spacing="8">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"
                                                           Foreground="{ThemeResource TextControlForeground}" />
                                                <TextBlock Text="{Binding Manufacturer}"
                                                           FontSize="12"
                                                           Foreground="{ThemeResource SystemBaseMediumColor}" />
                                                <TextBlock Text="{Binding DetailsText}"
                                                           FontSize="12"
                                                           Foreground="{ThemeResource SystemBaseMediumColor}"
                                                           TextWrapping="Wrap"
                                                           Visibility="{Binding HasDetails}" />
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Memory Section -->
                    <StackPanel Visibility="{Binding ShowMemory}"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=MemoryLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <ItemsControl ItemsSource="{Binding Memory}"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="8" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource MetadataCardStyle}">
                                        <StackPanel Spacing="6">
                                            <TextBlock Text="{Binding SizeDisplay}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{ThemeResource TextControlForeground}" />
                                            <TextBlock Text="{Binding TypeDisplay}"
                                                       FontSize="12"
                                                       Foreground="{ThemeResource SystemBaseMediumColor}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- GPUs Section -->
                    <StackPanel Visibility="{Binding ShowGpus}"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=GpusLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <ItemsControl ItemsSource="{Binding Gpus}"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="8" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Padding="0"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            Command="{Binding DataContext.NavigateToGpuCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0">
                                        <Border Style="{StaticResource MetadataCardStyle}">
                                            <StackPanel Spacing="8">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"
                                                           Foreground="{ThemeResource TextControlForeground}" />
                                                <TextBlock Text="{Binding Manufacturer}"
                                                           FontSize="12"
                                                           Foreground="{ThemeResource SystemBaseMediumColor}"
                                                           Visibility="{Binding HasManufacturer}" />
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Sound Synthesizers Section -->
                    <StackPanel Visibility="{Binding ShowSoundSynthesizers}"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=SoundSynthesizersLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <ItemsControl ItemsSource="{Binding SoundSynthesizers}"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="8" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Padding="0"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            Command="{Binding DataContext.NavigateToSoundSynthCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0">
                                        <Border Style="{StaticResource MetadataCardStyle}">
                                            <StackPanel Spacing="8">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"
                                                           Foreground="{ThemeResource TextControlForeground}" />
                                                <TextBlock Text="{Binding DetailsText}"
                                                           FontSize="12"
                                                           Foreground="{ThemeResource SystemBaseMediumColor}"
                                                           TextWrapping="Wrap"
                                                           Visibility="{Binding HasDetails}" />
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Storage Section -->
                    <StackPanel Visibility="{Binding ShowStorage}"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=StorageLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <ItemsControl ItemsSource="{Binding Storage}"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="8" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource MetadataCardStyle}">
                                        <StackPanel Spacing="6">
                                            <TextBlock Text="{Binding DisplayText}"
                                                       FontSize="13"
                                                       Foreground="{ThemeResource TextControlForeground}"
                                                       TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding TypeNote}"
                                                       FontSize="11"
                                                       Foreground="{ThemeResource SystemBaseMediumColor}"
                                                       TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Description Section -->
                    <StackPanel Visibility="{Binding ShowDescription}"
                                Spacing="8"
                                Margin="0,16,0,0">
                        <TextBlock Text="{Binding Source={StaticResource Strings}, Path=DescriptionSectionTitle}"
                                   FontSize="20"
                                   FontWeight="SemiBold" />
                        <local:MessageMarkdownView Markdown="{Binding DescriptionMarkdown}" />
                    </StackPanel>

                                </StackPanel>
                        </ScrollViewer>
                    </TabViewItem>

                    <TabViewItem Style="{StaticResource DetailTabViewItemStyle}"
                                 Header="{Binding Source={StaticResource Strings}, Path=SoftwareTabHeader}"
                                 Visibility="{Binding ShowSoftware}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Padding="0,16,0,0"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=MachineSoftwareLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <ItemsControl ItemsSource="{Binding Software}"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="0"
                                                HorizontalAlignment="Stretch" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Padding="0"
                                            Margin="0,0,0,8"
                                            MinHeight="70"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Stretch"
                                            HorizontalAlignment="Stretch"
                                            Command="{Binding DataContext.NavigateToSoftwareCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Grid MinHeight="70"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch">
                                                    <Border x:Name="SoftwareShadowBorder"
                                                            Style="{StaticResource MetadataCardStyle}"
                                                            Translation="0, 0, 4"
                                                            VerticalAlignment="Stretch">
                                                        <Border.Shadow>
                                                            <ThemeShadow />
                                                        </Border.Shadow>

                                                        <Grid ColumnSpacing="16">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>

                                                            <!-- Software Cover or Icon -->
                                                            <Grid Grid.Column="0"
                                                                  Width="54"
                                                                  Height="84"
                                                                  CornerRadius="4"
                                                                  Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                                                  VerticalAlignment="Center">
                                                                <Image Source="{Binding CoverImageUrl}"
                                                                       Stretch="Uniform"
                                                                       Width="54"
                                                                       Height="84"
                                                                       Visibility="{Binding HasCover, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                                <FontIcon Glyph="&#xE74C;"
                                                                          FontSize="28"
                                                                          Foreground="{ThemeResource SystemAccentColor}"
                                                                          HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"
                                                                          Visibility="{Binding HasCover, Converter={StaticResource InvertBoolToVisibilityConverter}}" />
                                                            </Grid>

                                                            <!-- Software Info -->
                                                            <StackPanel Grid.Column="1"
                                                                        Spacing="4"
                                                                        VerticalAlignment="Center"
                                                                        HorizontalAlignment="Stretch">
                                                                <TextBlock Text="{Binding Name}"
                                                                           FontSize="16"
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{ThemeResource TextControlForeground}"
                                                                           TextTrimming="CharacterEllipsis" />
                                                                <StackPanel Orientation="Horizontal"
                                                                            Spacing="12">
                                                                    <StackPanel Orientation="Horizontal"
                                                                                Spacing="6"
                                                                                Visibility="{Binding Family, Converter={StaticResource NullToVisibilityConverter}}">
                                                                        <FontIcon Glyph="&#xE8F1;"
                                                                                  FontSize="13"
                                                                                  Foreground="{ThemeResource SystemBaseMediumColor}" />
                                                                        <TextBlock Text="{Binding Family}"
                                                                                   FontSize="13"
                                                                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                                                                    </StackPanel>
                                                                </StackPanel>
                                                            </StackPanel>

                                                            <!-- Navigation Arrow -->
                                                            <StackPanel Grid.Column="2"
                                                                        VerticalAlignment="Center">
                                                                <FontIcon Glyph="&#xE72A;"
                                                                          FontSize="18"
                                                                          Foreground="{ThemeResource SystemAccentColor}" />
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>

                                                    <VisualStateManager.VisualStateGroups>
                                                        <VisualStateGroup x:Name="CommonStates">
                                                            <VisualState x:Name="Normal" />
                                                            <VisualState x:Name="PointerOver">
                                                                <VisualState.Setters>
                                                                    <Setter Target="SoftwareShadowBorder.Background"
                                                                            Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}" />
                                                                    <Setter Target="SoftwareShadowBorder.Translation"
                                                                            Value="0, -2, 8" />
                                                                </VisualState.Setters>
                                                            </VisualState>
                                                            <VisualState x:Name="Pressed">
                                                                <VisualState.Setters>
                                                                    <Setter Target="SoftwareShadowBorder.Background"
                                                                            Value="{ThemeResource CardBackgroundFillColorTertiaryBrush}" />
                                                                    <Setter Target="SoftwareShadowBorder.Translation"
                                                                            Value="0, 0, 2" />
                                                                </VisualState.Setters>
                                                            </VisualState>
                                                        </VisualStateGroup>
                                                    </VisualStateManager.VisualStateGroups>
                                                </Grid>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Content="{Binding Source={StaticResource Strings}, Path=PreviousPageButton}"
                                    Command="{Binding PreviousSoftwarePageCommand}"
                                    IsEnabled="{Binding CanGoToPreviousSoftwarePage}" />
                            <Button Grid.Column="1"
                                    Content="{Binding Source={StaticResource Strings}, Path=NextPageButton}"
                                    Command="{Binding NextSoftwarePageCommand}"
                                    IsEnabled="{Binding CanGoToNextSoftwarePage}" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding SoftwarePageSummary}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right" />
                        </Grid>
                    </StackPanel>
                        </ScrollViewer>
                    </TabViewItem>

                    <TabViewItem Style="{StaticResource DetailTabViewItemStyle}"
                                 Header="{Binding Source={StaticResource Strings}, Path=MediaTabHeader}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Padding="0,16,0,0"
                                Spacing="24">

                    <!-- Videos -->
                    <StackPanel Visibility="{Binding ShowVideos}"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=VideosLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <ItemsControl ItemsSource="{Binding Videos}"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="0"
                                                HorizontalAlignment="Stretch" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Padding="0"
                                            Margin="0,0,0,8"
                                            MinHeight="92"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Stretch"
                                            HorizontalAlignment="Stretch"
                                            Command="{Binding DataContext.OpenVideoCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Grid MinHeight="92"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch">
                                                    <Border x:Name="VideoShadowBorder"
                                                            Style="{StaticResource MetadataCardStyle}"
                                                            Translation="0, 0, 4"
                                                            VerticalAlignment="Stretch">
                                                        <Border.Shadow>
                                                            <ThemeShadow />
                                                        </Border.Shadow>

                                                        <Grid ColumnSpacing="16">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="120" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>

                                                            <Grid Grid.Column="0"
                                                                  Height="68"
                                                                  CornerRadius="6"
                                                                  Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                                                  VerticalAlignment="Center">
                                                                <Image Source="{Binding ThumbnailUrl}"
                                                                       Stretch="UniformToFill"
                                                                       Visibility="{Binding HasThumbnail, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                                <FontIcon Glyph="&#xE768;"
                                                                          FontSize="26"
                                                                          Foreground="{ThemeResource SystemAccentColor}"
                                                                          HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"
                                                                          Visibility="{Binding HasThumbnail, Converter={StaticResource InvertBoolToVisibilityConverter}}" />
                                                            </Grid>

                                                            <StackPanel Grid.Column="1"
                                                                        Spacing="4"
                                                                        VerticalAlignment="Center"
                                                                        HorizontalAlignment="Stretch">
                                                                <TextBlock Text="{Binding DisplayTitle}"
                                                                           FontSize="15"
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{ThemeResource TextControlForeground}"
                                                                           TextWrapping="WrapWholeWords"
                                                                           MaxLines="2"
                                                                           TextTrimming="CharacterEllipsis" />
                                                                <TextBlock Text="{Binding Provider}"
                                                                           FontSize="12"
                                                                           Foreground="{ThemeResource SystemBaseMediumColor}" />
                                                                <TextBlock Text="{Binding VideoId}"
                                                                           FontSize="11"
                                                                           Foreground="{ThemeResource SystemBaseMediumColor}"
                                                                           TextTrimming="CharacterEllipsis" />
                                                            </StackPanel>

                                                            <StackPanel Grid.Column="2"
                                                                        Spacing="6"
                                                                        VerticalAlignment="Center">
                                                                <FontIcon Glyph="&#xE8A7;"
                                                                          FontSize="18"
                                                                          HorizontalAlignment="Center"
                                                                          Foreground="{ThemeResource SystemAccentColor}" />
                                                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=OpenVideoButton}"
                                                                           FontSize="11"
                                                                           Foreground="{ThemeResource SystemAccentColor}"
                                                                           HorizontalAlignment="Center" />
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>

                                                    <VisualStateManager.VisualStateGroups>
                                                        <VisualStateGroup x:Name="CommonStates">
                                                            <VisualState x:Name="Normal" />
                                                            <VisualState x:Name="PointerOver">
                                                                <VisualState.Setters>
                                                                    <Setter Target="VideoShadowBorder.Background"
                                                                            Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}" />
                                                                    <Setter Target="VideoShadowBorder.Translation"
                                                                            Value="0, -2, 8" />
                                                                </VisualState.Setters>
                                                            </VisualState>
                                                            <VisualState x:Name="Pressed">
                                                                <VisualState.Setters>
                                                                    <Setter Target="VideoShadowBorder.Background"
                                                                            Value="{ThemeResource CardBackgroundFillColorTertiaryBrush}" />
                                                                    <Setter Target="VideoShadowBorder.Translation"
                                                                            Value="0, 0, 2" />
                                                                </VisualState.Setters>
                                                            </VisualState>
                                                        </VisualStateGroup>
                                                    </VisualStateManager.VisualStateGroups>
                                                </Grid>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Promo Art -->
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="8"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{Binding Source={StaticResource Strings}, Path=PromoArt}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource TextControlForeground}" />
                            <TextBlock Text="{Binding PromoArtCount}"
                                       FontSize="12"
                                       Foreground="{ThemeResource SystemBaseMediumColor}"
                                       Visibility="{Binding HasPromoArt, Converter={StaticResource BoolToVisibilityConverter}}" />
                        </StackPanel>

                        <Border Style="{StaticResource MetadataCardStyle}"
                                Visibility="{Binding HasPromoArt, Converter={StaticResource InvertBoolToVisibilityConverter}}">
                            <TextBlock Text="{Binding Source={StaticResource Strings}, Path=NoPromoArtForThisMachineYet}"
                                       FontSize="13"
                                       Foreground="{ThemeResource SystemBaseMediumColor}"
                                       TextWrapping="Wrap" />
                        </Border>

                        <ItemsControl ItemsSource="{Binding PromoArtGroups}"
                                      Visibility="{Binding HasPromoArt, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="16" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Spacing="10">
                                        <TextBlock Text="{Binding GroupName}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{ThemeResource TextControlForeground}" />
                                        <ItemsControl ItemsSource="{Binding Items}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Vertical"
                                                                Spacing="10" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Padding="0"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            HorizontalContentAlignment="Stretch"
                                                            HorizontalAlignment="Stretch"
                                                            Command="{Binding DataContext.ViewPromoArtDetailsCommand, ElementName=PageRoot}"
                                                            CommandParameter="{Binding PromoArtId}">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Grid HorizontalAlignment="Stretch">
                                                                    <Border x:Name="PromoArtShadowBorder"
                                                                            Style="{StaticResource MetadataCardStyle}"
                                                                            Padding="12"
                                                                            Translation="0, 0, 4">
                                                                        <Border.Shadow>
                                                                            <ThemeShadow />
                                                                        </Border.Shadow>

                                                                        <StackPanel Spacing="10">
                                                                            <Grid Height="220"
                                                                                  Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                                                                <Image Source="{Binding ThumbnailImageSource}"
                                                                                       Stretch="Uniform"
                                                                                       HorizontalAlignment="Center"
                                                                                       VerticalAlignment="Center"
                                                                                       Visibility="{Binding ThumbnailImageSource, Converter={StaticResource ObjectToVisibilityConverter}}" />
                                                                                <FontIcon Glyph="&#xE91B;"
                                                                                          FontSize="28"
                                                                                          Foreground="{ThemeResource SystemAccentColor}"
                                                                                          HorizontalAlignment="Center"
                                                                                          VerticalAlignment="Center"
                                                                                          Visibility="{Binding ThumbnailImageSource, Converter={StaticResource InverseObjectToVisibilityConverter}}" />
                                                                            </Grid>
                                                                            <TextBlock Text="{Binding Caption}"
                                                                                       FontSize="12"
                                                                                       Foreground="{ThemeResource SystemBaseMediumColor}"
                                                                                       TextWrapping="Wrap"
                                                                                       Visibility="{Binding HasCaption, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                                        </StackPanel>
                                                                    </Border>

                                                                    <VisualStateManager.VisualStateGroups>
                                                                        <VisualStateGroup x:Name="CommonStates">
                                                                            <VisualState x:Name="Normal" />
                                                                            <VisualState x:Name="PointerOver">
                                                                                <VisualState.Setters>
                                                                                    <Setter Target="PromoArtShadowBorder.Background"
                                                                                            Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}" />
                                                                                    <Setter Target="PromoArtShadowBorder.Translation"
                                                                                            Value="0, -2, 8" />
                                                                                </VisualState.Setters>
                                                                            </VisualState>
                                                                            <VisualState x:Name="Pressed">
                                                                                <VisualState.Setters>
                                                                                    <Setter Target="PromoArtShadowBorder.Background"
                                                                                            Value="{ThemeResource CardBackgroundFillColorTertiaryBrush}" />
                                                                                    <Setter Target="PromoArtShadowBorder.Translation"
                                                                                            Value="0, 0, 2" />
                                                                                </VisualState.Setters>
                                                                            </VisualState>
                                                                        </VisualStateGroup>
                                                                    </VisualStateManager.VisualStateGroups>
                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Photos Carousel (Last element before spacing) -->
                    <StackPanel Visibility="{Binding ShowPhotos}"
                                Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=PhotosLabel}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextControlForeground}" />
                        <wctui:Carousel ItemsSource="{Binding Photos}"
                                        Height="280"
                                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        HorizontalAlignment="Stretch">
                            <wctui:Carousel.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.ViewPhotoDetailsCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding PhotoId}"
                                            Padding="0"
                                            Background="Transparent">
                                        <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center">
                                            <Image Source="{Binding ThumbnailImageSource}"
                                                   Stretch="Uniform"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   MaxWidth="256"
                                                   MaxHeight="256" />
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </wctui:Carousel.ItemTemplate>
                        </wctui:Carousel>
                    </StackPanel>

                    <!-- Bottom Spacing -->
                    <Border Height="24" />

                    </StackPanel>
                        </ScrollViewer>
                    </TabViewItem>

                </TabView>

            </Grid>

        </Grid>

    </Grid>

</Page>

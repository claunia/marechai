<?xml version="1.0"
      encoding="utf-8"?>

<Page x:Class="Marechai.App.Presentation.Views.SoftwareListPage"
      x:Name="PageRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:loc="using:Marechai.App.Presentation.Converters"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header with Back Button and Title -->
        <Grid Grid.Row="0"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1"
              Padding="16,12,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" /> <ColumnDefinition Width="*" /> <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Command="{Binding GoBackCommand}"
                    Style="{ThemeResource AlternateButtonStyle}"
                    ToolTipService.ToolTip="Go back"
                    Padding="8"
                    MinWidth="44"
                    MinHeight="44"
                    VerticalAlignment="Center">
                <FontIcon Glyph="&#xE72B;"
                          FontSize="16" />
            </Button>

            <StackPanel Grid.Column="1"
                        Margin="16,0,0,0"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding PageTitle}"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{ThemeResource TextControlForeground}" />
                <TextBlock Text="{Binding FilterDescription}"
                           FontSize="12"
                           Foreground="{ThemeResource SystemBaseMediumColor}"
                           Margin="0,4,0,0" />
            </StackPanel>

            <ComboBox Grid.Column="2"
                      Header="{Binding Source={StaticResource Strings}, Path=SoftwareFilterByKindLabel}"
                      SelectedIndex="{Binding SelectedKindIndex, Mode=TwoWay}"
                      MinWidth="200"
                      Margin="16,0,0,0"
                      VerticalAlignment="Center">
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareAnyKindLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsSoftwareLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsOSLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsGameLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsDlcLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsSystemSoftwareLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsApplicationLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsDevelopmentSoftwareLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsServerSoftwareLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsMiddlewareLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsFirmwareLabel}" />
                <ComboBoxItem Content="{Binding Source={StaticResource Strings}, Path=SoftwareIsEmbeddedSoftwareLabel}" />
            </ComboBox>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">

            <!-- Loading State -->
            <StackPanel Visibility="{Binding IsLoading}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Padding="32"
                        Spacing="16">
                <ProgressRing IsActive="True"
                              IsIndeterminate="True"
                              Height="64"
                              Width="64"
                              Foreground="{ThemeResource SystemAccentColor}" />
                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=LoadingText}"
                           FontSize="14"
                           TextAlignment="Center"
                           Foreground="{ThemeResource SystemBaseMediumColor}" />
            </StackPanel>

            <!-- Error State -->
            <StackPanel Visibility="{Binding HasError}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Padding="24"
                        Spacing="16"
                        MaxWidth="400">
                <InfoBar IsOpen="True"
                         Severity="Error"
                         Title="{Binding Source={StaticResource Strings}, Path=SoftwareErrorInfoBar_Title}"
                         Message="{Binding ErrorMessage}"
                         IsClosable="False" />
                <Button Content="{Binding Source={StaticResource Strings}, Path=RetryButton}"
                        Command="{Binding LoadData}"
                        HorizontalAlignment="Center"
                        Style="{ThemeResource AccentButtonStyle}" />
            </StackPanel>

            <!-- Software List -->
            <Grid Visibility="{Binding IsDataLoaded}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <Grid Padding="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /> <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Count Header -->
                        <StackPanel Grid.Row="0"
                                    Padding="16,12"
                                    Orientation="Horizontal"
                                    Spacing="4">
                            <TextBlock FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource SystemBaseMediumColor}"
                                       Text="{Binding Source={StaticResource Strings}, Path=ResultsText}" />
                            <TextBlock FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource SystemBaseMediumColor}"
                                       Text="{Binding SoftwareList.Count}" />
                            <TextBlock FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource SystemBaseMediumColor}"
                                       Text="{Binding Source={StaticResource Strings}, Path=SoftwareLabel}" />
                        </StackPanel>

                        <!-- Software Items -->
                        <ItemsControl Grid.Row="1"
                                      ItemsSource="{Binding SoftwareList}"
                                      Padding="0"
                                      Margin="0,8,0,0"
                                      HorizontalAlignment="Stretch">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"
                                                Spacing="0"
                                                HorizontalAlignment="Stretch" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Padding="0"
                                            Margin="0,0,0,8"
                                            MinHeight="70"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Stretch"
                                            HorizontalAlignment="Stretch"
                                            Command="{Binding DataContext.NavigateToSoftwareCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Template="{StaticResource InteractiveCardButtonTemplate}">
                                        <Grid ColumnSpacing="16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Software Icon -->
                                            <Grid Grid.Column="0"
                                                  Width="48"
                                                  Height="48"
                                                  VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE74C;"
                                                          FontSize="28"
                                                          Foreground="{ThemeResource SystemAccentColor}"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                            </Grid>

                                            <!-- Software Info -->
                                            <StackPanel Grid.Column="1"
                                                        Spacing="4"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Stretch">
                                                <TextBlock Text="{Binding Name}"
                                                           FontSize="16"
                                                           FontWeight="SemiBold"
                                                           Foreground="{ThemeResource TextControlForeground}"
                                                           TextTrimming="CharacterEllipsis" />
                                                <StackPanel Orientation="Horizontal"
                                                            Spacing="12">
                                                    <Border Style="{StaticResource ChipInfoStyle}"
                                                            VerticalAlignment="Center"
                                                            Visibility="{Binding IsCompilation, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="{Binding Source={StaticResource Strings}, Path=SoftwareIsCompilationLabel}"
                                                                   Style="{StaticResource ChipTextStyle}"
                                                                   Foreground="{ThemeResource ChipInfoForegroundBrush}" />
                                                    </Border>
                                                    <StackPanel Orientation="Horizontal"
                                                                Spacing="6"
                                                                Visibility="{Binding Family, Converter={StaticResource NullToVisibilityConverter}}">
                                                        <FontIcon Glyph="&#xE8F1;"
                                                                  FontSize="13"
                                                                  Foreground="{ThemeResource SystemBaseMediumColor}" />
                                                        <TextBlock Text="{Binding Family}"
                                                                   FontSize="13"
                                                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                                                    </StackPanel>
                                                </StackPanel>
                                            </StackPanel>

                                            <!-- Navigation Arrow -->
                                            <StackPanel Grid.Column="2"
                                                        VerticalAlignment="Center">
                                                <FontIcon Glyph="&#xE72A;"
                                                          FontSize="18"
                                                          Foreground="{ThemeResource SystemAccentColor}" />
                                            </StackPanel>
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </Grid>
                </ScrollViewer>
            </Grid>

        </Grid>

    </Grid>

</Page>

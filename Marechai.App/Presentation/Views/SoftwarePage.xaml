<?xml version="1.0"
      encoding="utf-8"?>

<Page x:Class="Marechai.App.Presentation.Views.SoftwarePage"
      x:Name="PageRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:components="using:Marechai.App.Presentation.Components"
      xmlns:loc="using:Marechai.App.Presentation.Converters"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid utu:SafeArea.Insets="VisibleBounds">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <utu:NavigationBar Grid.Row="0"
                           Content="{Binding Path=Title}">
            <utu:NavigationBar.MainCommand>
                <AppBarButton Label="{Binding Source={StaticResource Strings}, Path=BackButton}" Icon="Back"
                              Command="{Binding GoBackCommand}"
                              AutomationProperties.Name="Go back" />
            </utu:NavigationBar.MainCommand>
        </utu:NavigationBar>

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="16"
                        Spacing="24">

                <!-- Software Count Display -->
                <StackPanel HorizontalAlignment="Center"
                            Spacing="8">
                    <TextBlock Text="{Binding SoftwareCountText}"
                               TextAlignment="Center"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource SystemBaseMediumColor}" />
                    <TextBlock Text="{Binding SoftwareCount}"
                               TextAlignment="Center"
                               FontSize="48"
                               FontWeight="Bold"
                               Foreground="{ThemeResource SystemAccentColor}" />
                </StackPanel>

                <!-- Loading State -->
                <StackPanel Visibility="{Binding IsLoading}"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Padding="32"
                            Spacing="16">
                    <ProgressRing IsActive="True"
                                  IsIndeterminate="True"
                                  Height="48"
                                  Width="48" />
                    <TextBlock
                               Text="{Binding Source={StaticResource Strings}, Path=LoadingText}"
                               TextAlignment="Center"
                               FontSize="14" />
                </StackPanel>

                <!-- Error State -->
                <StackPanel Visibility="{Binding HasError}"
                            Padding="16"
                            Background="{ThemeResource SystemErrorBackgroundColor}"
                            CornerRadius="8"
                            Spacing="8">
                    <TextBlock
                               Text="{Binding Source={StaticResource Strings}, Path=ErrorTitle}"
                               FontWeight="Bold"
                               Foreground="{ThemeResource SystemErrorTextForegroundColor}" />
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{ThemeResource SystemErrorTextForegroundColor}"
                               TextWrapping="Wrap" />
                </StackPanel>

                <!-- Main Content (visible when loaded and no error) -->
                <StackPanel Visibility="{Binding IsDataLoaded}"
                            Spacing="24">

                    <!-- Letters Grid Section -->
                    <StackPanel Spacing="12">
                        <TextBlock
                                   Text="{Binding Source={StaticResource Strings}, Path=BrowseByLetterText}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                        <ItemsRepeater ItemsSource="{Binding LettersList}"
                                       Layout="{StaticResource LettersGridLayout}">
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <components:KeyButton Content="{Binding}"
                                            Command="{Binding DataContext.NavigateByLetterCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            />
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                    <!-- Platforms Grid Section -->
                    <StackPanel Spacing="12">
                        <TextBlock Text="{Binding PlatformsGridTitle}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                        <ItemsRepeater ItemsSource="{Binding PlatformsList}"
                                       Layout="{StaticResource PlatformsGridLayout}">
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <components:KeyButton Content="{Binding Name}"
                                            Command="{Binding DataContext.NavigateByPlatformCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}" />
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                    <!-- Specifications Section -->
                    <StackPanel Spacing="12"
                                Visibility="{Binding SpecificationsGridTitle, Converter={StaticResource StringToVisibilityConverter}}">
                        <TextBlock Text="{Binding SpecificationsGridTitle}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                        <ItemsRepeater ItemsSource="{Binding SpecificationGroups}">
                            <ItemsRepeater.Layout>
                                <StackLayout Spacing="12" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Spacing="8">
                                        <TextBlock Text="{Binding DisplayKey}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                                        <ItemsRepeater ItemsSource="{Binding Values}"
                                                       Layout="{StaticResource SpecValuesGridLayout}">
                                            <ItemsRepeater.ItemTemplate>
                                                <DataTemplate>
                                                    <components:KeyButton Content="{Binding DisplayValue}"
                                                            Command="{Binding DataContext.NavigateBySpecCommand, ElementName=PageRoot}"
                                                            CommandParameter="{Binding}" />
                                                </DataTemplate>
                                            </ItemsRepeater.ItemTemplate>
                                        </ItemsRepeater>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                    <!-- Years Grid Section -->
                    <StackPanel Spacing="12">
                        <TextBlock Text="{Binding YearsGridTitle}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource SystemBaseMediumColor}" />
                        <ItemsRepeater ItemsSource="{Binding YearsList}"
                                       Layout="{StaticResource YearsGridLayout}">
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <components:KeyButton Content="{Binding}"
                                            Command="{Binding DataContext.NavigateByYearCommand, ElementName=PageRoot}"
                                            CommandParameter="{Binding}"
                                            />
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                    <!-- All Software Button -->
                    <StackPanel Spacing="12">
                        <Button
                                Content="{Binding Source={StaticResource Strings}, Path=AllSoftwareButton}"
                                Padding="16,12"
                                HorizontalAlignment="Stretch"
                                FontSize="16"
                                FontWeight="SemiBold"
                                Command="{Binding NavigateAllSoftwareCommand}"
                                Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>

                </StackPanel>

            </StackPanel>
        </ScrollViewer>

    </Grid>

    <Page.Resources>
        <!-- Responsive Grid Layouts -->
        <UniformGridLayout x:Key="LettersGridLayout"
                           ItemsStretch="Fill"
                           MinItemWidth="44"
                           MinItemHeight="60"
                           MaximumRowsOrColumns="13" />

        <UniformGridLayout x:Key="PlatformsGridLayout"
                           ItemsStretch="Fill"
                           MinItemWidth="80"
                           MinItemHeight="60"
                           MaximumRowsOrColumns="6" />

        <UniformGridLayout x:Key="YearsGridLayout"
                           ItemsStretch="Fill"
                           MinItemWidth="54"
                           MinItemHeight="60"
                           MaximumRowsOrColumns="10" />

        <UniformGridLayout x:Key="SpecValuesGridLayout"
                           ItemsStretch="Fill"
                           MinItemWidth="80"
                           MinItemHeight="60"
                           MaximumRowsOrColumns="6" />

    </Page.Resources>

</Page>

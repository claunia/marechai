<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Marechai.App.Presentation.Views.MagazinesPage"
      x:Name="PageRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid utu:SafeArea.Insets="VisibleBounds">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <utu:NavigationBar Grid.Row="0" Content="{Binding Path=Title}">
            <utu:NavigationBar.MainCommand>
                <AppBarButton Label="{Binding Source={StaticResource Strings}, Path=BackButton}" Icon="Back"
                              Command="{Binding GoBackCommand}" AutomationProperties.Name="Go back" />
            </utu:NavigationBar.MainCommand>
        </utu:NavigationBar>
        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="16" Spacing="24">
                <StackPanel HorizontalAlignment="Center" Spacing="8">
                    <TextBlock Text="{Binding MagazineCountText}" TextAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource SystemBaseMediumColor}" />
                    <TextBlock Text="{Binding MagazineCount}" TextAlignment="Center" FontSize="48" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" />
                </StackPanel>
                <StackPanel Visibility="{Binding IsLoading}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="32" Spacing="16">
                    <ProgressRing IsActive="True" IsIndeterminate="True" Height="48" Width="48" />
                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=LoadingText}" TextAlignment="Center" FontSize="14" />
                </StackPanel>
                <StackPanel Visibility="{Binding HasError}" Padding="16" Background="{ThemeResource SystemErrorBackgroundColor}" CornerRadius="8" Spacing="8">
                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=ErrorTitle}" FontWeight="Bold" Foreground="{ThemeResource SystemErrorTextForegroundColor}" />
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{ThemeResource SystemErrorTextForegroundColor}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Visibility="{Binding IsDataLoaded}" Spacing="24">
                    <StackPanel Spacing="12">
                        <TextBlock Text="{Binding Source={StaticResource Strings}, Path=BrowseByLetterText}" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource SystemBaseMediumColor}" />
                        <ItemsRepeater ItemsSource="{Binding LettersList}" Layout="{StaticResource LettersGridLayout}">
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}" Command="{Binding DataContext.NavigateByLetterCommand, ElementName=PageRoot}" CommandParameter="{Binding}" Style="{StaticResource LetterYearButtonStyle}" />
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                    <StackPanel Spacing="12">
                        <TextBlock Text="{Binding YearsGridTitle}" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource SystemBaseMediumColor}" />
                        <ItemsRepeater ItemsSource="{Binding YearsList}" Layout="{StaticResource YearsGridLayout}">
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}" Command="{Binding DataContext.NavigateByYearCommand, ElementName=PageRoot}" CommandParameter="{Binding}" Style="{StaticResource LetterYearButtonStyle}" />
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                    <StackPanel Spacing="12">
                        <Button Content="{Binding Source={StaticResource Strings}, Path=AllMagazinesButton}" Padding="16,12" HorizontalAlignment="Stretch" FontSize="16" FontWeight="SemiBold" Command="{Binding NavigateAllMagazinesCommand}" Style="{StaticResource AccentButtonStyle}" />
                        <TextBox PlaceholderText="{Binding Source={StaticResource Strings}, Path=MagazinesSearchBox_Placeholder}" Padding="12" IsEnabled="False" Opacity="0.5" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
    <Page.Resources>
        <UniformGridLayout x:Key="LettersGridLayout" ItemsStretch="Fill" MinItemWidth="44" MinItemHeight="44" MaximumRowsOrColumns="13" />
        <UniformGridLayout x:Key="YearsGridLayout" ItemsStretch="Fill" MinItemWidth="54" MinItemHeight="44" MaximumRowsOrColumns="10" />
    </Page.Resources>
</Page>
